package simplelookup.listener;

/**
 * Base LookupListener interface provides no functionality but to act as an
 * umbrella type for the other LookupListener interfaces.
 * 
 * @author Robert Wapshott
 */
public interface LookupListener<T> {
}
